//ENTRY(name, parent),
//ENTRY(name, name), // a boolean tick
// obtain a MenuSelection by MENU(name) macro in .cpp (#include "plugin.hpp")
ENTRY(Null, Null),  // null terminal parent for all (separates as a bool)
